{
  "name": "chm-doc-generator",
  "version": "1.0.0",
  "main": "dist-electron/main/index.js",
  "description": "专业的CHM帮助文档生成解决方案",
  "author": "国民技术",
  "license": "MIT",
  "private": true,
  "keywords": [
    "electron",
    "rollup",
    "vite",
    "vue3",
    "vue"
  ],
  "debug": {
    "env": {
      "VITE_DEV_SERVER_URL": "http://127.0.0.1:3344/"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build && electron-builder && node build.js",
    "build:win": "vue-tsc --noEmit && vite build && electron-builder --win && node build.js",
    "build:mac": "vue-tsc --noEmit && vite build && electron-builder --mac && node build.js",
    "build:linux": "vue-tsc --noEmit && vite build && electron-builder --linux && node build.js",
    "dist": "vue-tsc --noEmit && vite build && electron-builder && node build.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vitejs/plugin-vue": "^5.0.4",
    "@vueuse/core": "^13.9.0",
    "@vueuse/electron": "14.0.0-alpha.0",
    "autoprefixer": "^10.4.21",
    "electron": "^29.4.6",
    "electron-builder": "^24.13.3",
    "postcss": "^8.5.6",
    "sass": "^1.92.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.4.2",
    "vite": "^5.1.5",
    "vite-plugin-electron": "^0.28.4",
    "vite-plugin-electron-renderer": "^0.14.5",
    "vue": "^3.4.21",
    "vue-tsc": "^2.0.6",
    "markdown-it": "^14.1.0",
    "@types/markdown-it": "^14.1.2"
  },
  "dependencies": {
    "dayjs": "^1.11.18",
    "lucide-vue-next": "^0.543.0",
    "vue-router": "^4.5.1"
  }
}
